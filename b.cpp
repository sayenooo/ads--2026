#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, x;
    cin >> n;

    vector<int> v;
    deque<int> dq;

    for (int i = 0; i < n; i++){
        cin >> x;
        v.push_back(x);
    }

    for (int i = 0; i < v.size(); i++){
        while(!dq.empty() && )   

    }
    
    while (!q.empty()){
        cout << q.front() << ' ';
        q.pop();
    }
    return 0;

}